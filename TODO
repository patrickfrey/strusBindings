Fix CodeProject Articles
Delete unused Docker images
Publish Docker images on webpage and refurbish installation page

If binding language data structure has members serialize them too and define interfaces with members instead of dictionary for return values (interface addressed by argument of OPEN)

Check multiple propagation of flags like -O0 -O2 -O3 for the same target (remove silly STRUS_OPTIMIZATION_LEVEL)

In XML/JSON call interface call papuga in steps, returning a breakpoint id each step
NodeJS Bindings

