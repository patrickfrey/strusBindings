statserver = {
	database = string,
	statsproc = string,
	autocompact = boolean,
	cache = integer,
	compression = boolean,
	max_open_files = integer,
	write_buffer_size = integer,
	block_size = integer
	storagelinks = {string}
}

storagelink = {
	link = {string}
}

statisticsblob = {
	timestamp = string,
	blob = string
}

statisticsquery = {
	termstats = {{
		type = string,
		value = string
	}}
}

statisticsfetch = {
	timestamp = string
}

statistics = {
	termstats = {{
		type = string,
		value = string,
		df = number
	}},
	globalstats = {
		nofdocs = integer
	}
}

