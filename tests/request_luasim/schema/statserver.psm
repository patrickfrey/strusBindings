statserver = {
	storagelinks = {string},
	blocks = integer,
	proc = string
}

statisticsblob = {
	timestamp = string,
	blob = string
}

statisticsquery = {
	termstats = {{
		type = string,
		value = string
	}}
}

statisticsfetch = {
	timestamp = string
}

statistics = {
	termstats = {{
		type = string,
		value = string,
		df = number
	}},
	globalstats = {
		nofdocs = integer
	}
}

