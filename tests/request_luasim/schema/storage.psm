storageAddress = {
	database = string
}

metadata = {
	update = {{
		op = string,
		name = string,
		type = string,
		oldname = string
	}}
}

storage = {
	database = string,
	statsproc = string,
	autocompact = boolean,
	acl = boolean,
	cachedterms = string,
	cache = integer,
	compression = boolean,
	max_open_files = integer,
	write_buffer_size = integer,
	block_size = integer
}

