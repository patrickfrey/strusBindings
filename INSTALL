git submodule update --init --recursive
git submodule foreach git checkout master
git submodule foreach git pull

mkdir build
cd build
cmake -DWITH_STRUS_VECTOR="YES" -DWITH_STRUS_PATTERN="YES" ..	
make -j<N>
make test ARGS=-j<N>

